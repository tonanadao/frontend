export declare class SyncValue<T> {
    private _updater;
    private _value;
    private _sync;
    constructor(initial: T, updater: (value: T) => Promise<void>);
    get value(): T;
    set value(v: T);
}
