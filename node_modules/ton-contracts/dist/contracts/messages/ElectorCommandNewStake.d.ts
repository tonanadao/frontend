/// <reference types="node" />
import BN from "bn.js";
import { Cell, Message } from "ton";
export declare class ElectorCommandNewStake implements Message {
    private readonly queryId;
    private readonly publicKey;
    private readonly stakeAt;
    private readonly maxFactor;
    private readonly adnlAddress;
    private readonly signature;
    constructor(args: {
        queryId: BN;
        publicKey: Buffer;
        stakeAt: number;
        maxFactor: number;
        adnlAddress: Buffer;
        signature: Buffer;
    });
    writeTo(cell: Cell): void;
}
