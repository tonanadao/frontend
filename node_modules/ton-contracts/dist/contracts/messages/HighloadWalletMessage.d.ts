import { Cell, InternalMessage, Message, SendMode } from "ton";
export declare class HighloadWalletMessage implements Message {
    readonly seqno: number;
    readonly walletId: number;
    readonly timeout: number;
    readonly messages: {
        sendMode: SendMode;
        order: InternalMessage;
    }[];
    constructor(args: {
        seqno: number;
        walletId: number;
        timeout: number;
        messages: {
            sendMode: SendMode;
            order: InternalMessage;
        }[];
    });
    writeTo(cell: Cell): void;
}
