import { Address, Contract, TonClient, ContractSource } from "ton";
import { NominatorPoolSource } from "./NominatorPoolSource";
export declare class NominatorPool implements Contract {
    static create(client: TonClient, source: NominatorPoolSource): Promise<NominatorPool>;
    readonly source: ContractSource;
    readonly address: Address;
    readonly client: TonClient;
    private constructor();
    deploy(): Promise<void>;
    getNominatorsList(): Promise<void>;
}
