import { Address, Contract, ContractSource, TonClient } from "ton";
import { LoggerContractSource } from "./LoggerContractSource";
export declare class LoggerContract implements Contract {
    static create(client: TonClient, source: LoggerContractSource): Promise<LoggerContract>;
    static createRandom(client: TonClient, workchain?: number): Promise<LoggerContract>;
    readonly source: ContractSource;
    readonly address: Address;
    readonly client: TonClient;
    private constructor();
    getSeqno(): Promise<number>;
    getExternalMessages(): Promise<import("ton").TonTransaction[]>;
    getInternalMessages(): Promise<import("ton").TonTransaction[]>;
}
