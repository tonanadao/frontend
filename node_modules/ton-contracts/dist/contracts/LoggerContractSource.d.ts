/// <reference types="node" />
import { Cell, ContractSource } from "ton";
export declare class LoggerContractSource implements ContractSource {
    static readonly SOURCE: Buffer;
    static create(opts: {
        seed: Buffer;
        workchain: number;
    }): LoggerContractSource;
    static restore(backup: string): LoggerContractSource;
    readonly initialCode: Cell;
    readonly initialData: Cell;
    readonly workchain: number;
    readonly seed: Buffer;
    readonly type = "org.ton.logger";
    private constructor();
    describe(): string;
    backup(): string;
}
