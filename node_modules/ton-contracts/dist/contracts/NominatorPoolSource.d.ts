/// <reference types="node" />
import { Address, Cell, ContractSource } from "ton";
export declare class NominatorPoolSource implements ContractSource {
    static readonly SOURCE: Buffer;
    static create(opts: {
        owner: Address;
        workchain: number;
        seed?: Buffer;
    }): NominatorPoolSource;
    static restore(backup: string): NominatorPoolSource;
    readonly initialCode: Cell;
    readonly initialData: Cell;
    readonly workchain: number;
    readonly seed: Buffer;
    readonly owner: Address;
    readonly type = "con.tonwhales.nominators.v1";
    private constructor();
    describe(): string;
    backup(): string;
}
