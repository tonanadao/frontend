/// <reference types="node" />
import { Cell, ContractSource } from "ton";
import { Maybe } from "ton/dist/types";
export declare class HighloadWalletSource implements ContractSource {
    static readonly SOURCE: Buffer;
    static create(opts: {
        workchain: number;
        publicKey: Buffer;
        walletId?: Maybe<number>;
    }): HighloadWalletSource;
    static restore(backup: string): HighloadWalletSource;
    readonly initialCode: Cell;
    readonly initialData: Cell;
    readonly walletId: number;
    readonly workchain: number;
    readonly publicKey: Buffer;
    readonly type = "org.ton.wallets.highload";
    private constructor();
    describe(): string;
    backup(): string;
}
